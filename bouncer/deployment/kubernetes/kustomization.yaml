apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bouncer

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

labels:
  - pairs:
      app.kubernetes.io/name: bouncer
      app.kubernetes.io/component: ocsp-server
      app.kubernetes.io/part-of: gaia
