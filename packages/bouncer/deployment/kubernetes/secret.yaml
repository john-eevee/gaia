apiVersion: v1
kind: Secret
metadata:
  name: bouncer-secret
  namespace: bouncer
  labels:
    app: bouncer
type: Opaque
stringData:
  # Replace with actual password - consider using sealed secrets or external secrets operator
  DB_PASSWORD: "change_this_secure_password"
