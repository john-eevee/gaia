# Gaia Website (Docusaurus)

This folder contains a Docusaurus v2 site replacing the previous Hugo setup.

Quick start:

```bash
cd website
npm install
npm run start
```

Build:

```bash
npm run build
```
